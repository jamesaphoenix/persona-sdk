{
  "name": "@persona-sdk/bun-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "command -v bun >/dev/null 2>&1 && bun test || echo 'Bun not available, skipping bun tests'",
    "test:record": "CASSETTE_MODE=record bun test",
    "test:replay": "CASSETTE_MODE=replay bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "command -v bun >/dev/null 2>&1 && bun test --coverage || echo 'Bun not available, skipping bun coverage'",
    "bench": "bun run src/benchmark.ts",
    "clean": "rm -rf cassettes/*.json signatures/*.json"
  },
  "dependencies": {
    "@jamesaphoenix/persona-sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}